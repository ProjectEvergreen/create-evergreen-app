// import './header';

describe('This is just a test', () => {
  
  it('should be true', () => {
    const sum = 1 + 1;

    expect(sum).toBe(2);
  });

});

describe('Header Component', () => {

  describe('Default Behavior', () => {
    
    it('should have a greeting', () => { 

    });

    xit('should have a logo', () => { 

    });

    xit('should have a link', () => { 

    });
  });

});